\"\"\"Database Models for ASTRA AI Business Generator\"\"\"\nfrom datetime import datetime\nfrom sqlalchemy import Column, Integer, String, Float, DateTime, Boolean, Text, ForeignKey, Enum\nfrom sqlalchemy.orm import relationship\nimport enum\n\nfrom app.db.database import Base\n\n\nclass UserRole(str, enum.Enum):\n    \"\"\"User roles\"\"\"\n    ADMIN = \"admin\"\n    USER = \"user\"\n    PREMIUM = \"premium\"\n\n\nclass BusinessStatus(str, enum.Enum):\n    \"\"\"Business status\"\"\"\n    ACTIVE = \"active\"\n    PAUSED = \"paused\"\n    ARCHIVED = \"archived\"\n    FAILED = \"failed\"\n\n\nclass PaymentStatus(str, enum.Enum):\n    \"\"\"Payment status\"\"\"\n    PENDING = \"pending\"\n    SUCCESS = \"success\"\n    FAILED = \"failed\"\n    REFUNDED = \"refunded\"\n\n\nclass User(Base):\n    \"\"\"User model\"\"\"\n    __tablename__ = \"users\"\n    \n    id = Column(Integer, primary_key=True)\n    email = Column(String(255), unique=True, index=True)\n    username = Column(String(255), unique=True, index=True)\n    hashed_password = Column(String(255))\n    full_name = Column(String(255))\n    role = Column(Enum(UserRole), default=UserRole.USER)\n    is_active = Column(Boolean, default=True)\n    is_verified = Column(Boolean, default=False)\n    created_at = Column(DateTime, default=datetime.utcnow)\n    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)\n    \n    # Relationships\n    businesses = relationship(\"Business\", back_populates=\"owner\")\n    payments = relationship(\"Payment\", back_populates=\"user\")\n    analytics = relationship(\"Analytics\", back_populates=\"user\")\n\n\nclass Business(Base):\n    \"\"\"Business model\"\"\"\n    __tablename__ = \"businesses\"\n    \n    id = Column(String(50), primary_key=True, index=True)\n    user_id = Column(Integer, ForeignKey(\"users.id\"))\n    name = Column(String(255))\n    market = Column(String(255))\n    niche = Column(String(255))\n    description = Column(Text)\n    product_description = Column(Text)\n    landing_page = Column(Text)\n    status = Column(Enum(BusinessStatus), default=BusinessStatus.ACTIVE)\n    revenue = Column(Float, default=0.0)\n    customers = Column(Integer, default=0)\n    conversion_rate = Column(Float, default=0.0)\n    monthly_growth = Column(Float, default=0.0)\n    created_at = Column(DateTime, default=datetime.utcnow)\n    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)\n    \n    # Relationships\n    owner = relationship(\"User\", back_populates=\"businesses\")\n    products = relationship(\"Product\", back_populates=\"business\")\n    content = relationship(\"Content\", back_populates=\"business\")\n    analytics = relationship(\"Analytics\", back_populates=\"business\")\n\n\nclass Product(Base):\n    \"\"\"Product model\"\"\"\n    __tablename__ = \"products\"\n    \n    id = Column(String(50), primary_key=True)\n    business_id = Column(String(50), ForeignKey(\"businesses.id\"))\n    name = Column(String(255))\n    description = Column(Text)\n    price = Column(Float)\n    currency = Column(String(3), default=\"EUR\")\n    stripe_product_id = Column(String(255))\n    created_at = Column(DateTime, default=datetime.utcnow)\n    \n    # Relationships\n    business = relationship(\"Business\", back_populates=\"products\")\n\n\nclass Content(Base):\n    \"\"\"Content model\"\"\"\n    __tablename__ = \"content\"\n    \n    id = Column(String(50), primary_key=True)\n    business_id = Column(String(50), ForeignKey(\"businesses.id\"))\n    content_type = Column(String(50))  # tiktok, blog, email, seo\n    title = Column(String(255))\n    body = Column(Text)\n    status = Column(String(50), default=\"draft\")  # draft, published, archived\n    created_at = Column(DateTime, default=datetime.utcnow)\n    published_at = Column(DateTime)\n    \n    # Relationships\n    business = relationship(\"Business\", back_populates=\"content\")\n\n\nclass Payment(Base):\n    \"\"\"Payment model\"\"\"\n    __tablename__ = \"payments\"\n    \n    id = Column(String(50), primary_key=True)\n    user_id = Column(Integer, ForeignKey(\"users.id\"))\n    stripe_session_id = Column(String(255))\n    paypal_transaction_id = Column(String(255))\n    amount = Column(Float)\n    currency = Column(String(3), default=\"EUR\")\n    status = Column(Enum(PaymentStatus), default=PaymentStatus.PENDING)\n    customer_email = Column(String(255))\n    created_at = Column(DateTime, default=datetime.utcnow)\n    completed_at = Column(DateTime)\n    \n    # Relationships\n    user = relationship(\"User\", back_populates=\"payments\")\n\n\nclass Analytics(Base):\n    \"\"\"Analytics model\"\"\"\n    __tablename__ = \"analytics\"\n    \n    id = Column(Integer, primary_key=True)\n    user_id = Column(Integer, ForeignKey(\"users.id\"))\n    business_id = Column(String(50), ForeignKey(\"businesses.id\"))\n    daily_revenue = Column(Float, default=0.0)\n    daily_customers = Column(Integer, default=0)\n    conversion_rate = Column(Float, default=0.0)\n    traffic = Column(Integer, default=0)\n    date = Column(DateTime, default=datetime.utcnow)\n    \n    # Relationships\n    user = relationship(\"User\", back_populates=\"analytics\")\n    business = relationship(\"Business\", back_populates=\"analytics\")\n"